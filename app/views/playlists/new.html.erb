<h1>Creating New Playlist</h1>

<%= form_with scope: :playlist, url: playlists_path, local: true do |form| %>

  <p>
    <%= form.label :name %><br>
    <%= form.text_area :name %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>



<h1>Delete playlist</h1>
<%= form_with scope: :playlist, url: playlists_path, local: true do |form| %>

  <%= form.collection_select :playlist_id, @playlist, :id, :name %>

  <p>
    <%= form.submit %>
  </p>
<% end %>

<%= link_to 'Back', playlists_path %>